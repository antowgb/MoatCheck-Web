fastapi>=0.115
uvicorn[standard]>=0.30
yfinance>=0.2.50
pandas>=2.2
numpy>=1.26
supabase>=2.6
python-dotenv>=1.0
pydantic>=2.7
pytest>=8.0
pytest-cov>=5.0
scipy>=1.11
# V2 qualitative layer
groq>=0.11              # Groq SDK — event classification (JSON mode)
feedparser>=6.0         # RSS/Atom parsing (IR feeds + generalist press)
# Gmail newsletter collector (optional at runtime — only needed if the
# 'newsletter' source flag is enabled; see GMAIL_SETUP.md)
google-api-python-client>=2.100
google-auth>=2.30
google-auth-oauthlib>=1.2
